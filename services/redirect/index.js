/* eslint-disable no-undef */
if (typeof window === 'object' && window.location && (/experteer/.test(window.location.hostname) || (/careerlift/.test(window.location.hostname)))) {
  window.location = 'https://google.com';
}
/* eslint-enable no-undef */
